---
import Layout from '@/layouts/Layout.astro'
import Card from '@/components/Card.astro'
---

<Layout>
	<main
		class="w-[80vw] mx-[10vw] mb-5 grid sm:grid-cols-2 md:grid-cols-3 xl:grid-cols gap-5 xl:h-[1180px]"
	>
		{[1,2,3,4,5].map(n => (
			<Card num={n} />
		))}
	</main>
</Layout>